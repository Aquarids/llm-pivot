[pytest]
markers =
    integration: Integration tests that require real services
    api: Tests for online API models
    local: Tests for local models
    slow: Slow running tests

addopts = -v --tb=short -s
testpaths = tests

log_cli = true
log_cli_level = INFO
